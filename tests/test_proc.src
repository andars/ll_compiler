# EXPECT: 4
(define (foo 1)
    (alloc 1)
    (set (local 0) (bar 2))
    (return (local 0)))

(define (bar 1)
    (alloc 1)
    (return (param 0)))

(define (main 0)
    (alloc 3)
    (set (local 0) (+ 5 2) )
    (set (local 1) (local 0))
    (set (local 2) (local 1))
    (set (local 2) (+ (local 1) 1))
    (set (local 0) (foo 27))
    (set (local 1) (foo 28))
    (set (local 1) (+ (local 0) (local 1)))
    (return (local 1)))
